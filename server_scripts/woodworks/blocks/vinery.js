// priority: 5

// Vinery wooden blocks

const lattice = modBlockFactory('vinery', 'lattice', 8, 2);
const v_chair = modBlockFactory('vinery', 'chair', 4, 1);
const v_table = modBlockFactory('vinery', 'table', 2, 0);